# Local DB file path
LOCAL_DB_PATH = "file:app/db/data.sqlite"

# Secret key to isolate session data
SESSION_KEY = my-secret-key

# Flask host and port
FLASK_RUN_HOST = localhost
FLASK_RUN_PORT = 5000
FLASK_DEBUG    = True
